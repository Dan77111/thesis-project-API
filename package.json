{
  "name": "project-api",
  "version": "1.0.0",
  "description": "An API that periodically fetches data from the eurostat API and elaborates it for the related React frontend app. It also periodically saves a snapshot of the data on a DB.",
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "start": "node .",
    "predebug": "npm run build",
    "debug": "npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniele Dalla Rosa",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.12",
    "node-fetch": "^2.0.0",
    "node-schedule": "^2.0.0",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.7",
    "@types/node-fetch": "^2.5.12",
    "@types/node-schedule": "^1.3.2",
    "tslint": "^6.1.3",
    "typescript": "^4.4.4"
  }
}
